#!/bin/bash

# launching the desktop with Singularity
# NOTE that this file should be placed in Singularity module thus having the
# same life span as Singularity does.

singularity run -H $HOME -B /projects:/projects -B /scratch:/scratch -B /cm:/cm:ro \
